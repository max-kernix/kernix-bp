//// Manage on reisze events & functions calls

/// Imports
// Import lodash.throttle
// var _throttle = require('lodash/throttle');


// doc.ready

  // Functions initial calls, when docment ready
    // allResizeFunctions()
    // when done > no FOUC proc

  // Max 0.2s spam on resize event
  // jQuery(window).on('resize', _throttle(function(true) { // true > calls when ends // check syntax
    // allResizeFunctions()

  // }, 200));

// allResizeFunctions()
